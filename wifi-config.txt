# WiFi Configuration for Bible Clock
# Network: Home
# Password: govikings

# For NetworkManager (most Linux systems):
# To connect to WiFi, run:
# sudo nmcli device wifi connect "Home" password "govikings"

# For wpa_supplicant configuration (/etc/wpa_supplicant/wpa_supplicant.conf):
network={
    ssid="Home"
    psk="govikings"
    key_mgmt=WPA-PSK
}

# For Raspberry Pi (/etc/wpa_supplicant/wpa_supplicant.conf):
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
country=US

network={
    ssid="Home"
    psk="govikings"
}

# Note: This file contains WiFi credentials and should be secured
# chmod 600 wifi-config.txt